language: generic

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq pdfgrep
 - echo $(pdfgrep --version)
before_script:
 - chmod +x _scripts/oficial.sh

script:
 - ./_scripts/oficial.sh

after_script:
 - cat /tmp/findornot.log

branches:
  only:
    - master
    - develop
